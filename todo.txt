UI Setup: Tk window + Entry(HH:MM) + Entry(message) + Add button + optional Listbox.

Add Reminder: Replace input() with Entries; update CSV + in-memory lists.

Background Loop: Run alarm check in threading.Thread(daemon=True); use triggered logic.

Notifications: Popup (messagebox) or update Label; schedule via root.after() from thread.

Thread Safety: Never update widgets directly from background thread.

Optional: Delete alarms, auto-remove past alarms, dynamic CSV reload.

Test: Multiple alarms/minute, adding while running, UI responsiveness.